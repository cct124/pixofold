# AGENTS.md

## 项目与入口

- PixoFold（轻图）是本地批量图片压缩工具，计划支持 PNG、JPEG、GIF、APNG，采用 GPL-3.0-or-later。已建立 Tauri 2、React、TypeScript 与独立 Rust 核心的脚手架；压缩引擎、任务队列及正式业务界面尚未实现。
- 从 [README.md](README.md) 了解项目；架构与引擎选型见[项目方案](docs/架构设计文档/pixofold-proposal.md)，交互规则见[UI 交互设计](docs/架构设计文档/ui-interaction-design.md)。
- 界面以 [HTML 原型](docs/UI界面设计/PixoFold.html)及[原型说明](docs/UI界面设计/HTML原型说明.md)为准，生成的 UI 图片仅作历史参考。旧项目 png-palettes 用于参考，未经任务要求不修改。
- 实际工具链与命令以 README、`package.json`、`rust-toolchain.toml` 为准；不把规划或未运行的跨平台 CI 写成已验证能力。

## 接手与修改

- 先检查分支与工作区改动、相关目录的 `AGENTS.md`；已有 `docs/devlog/README.md` 时，读取索引和对应任务记录，再核对代码。
- 先检索、后读取、再编辑：用 `rg -n` 定位定义与引用，用 `rg --files` 找文件，按需读取相关区间及必要上下文。
- 只修改任务相关内容，保留已有改动，优先使用 patch；修改接口或行为前检查调用关系，同步所属文档与验收标准。
- HTML 原型只模拟压缩，不上传、生成或覆盖图片；真实压缩接入后，大小、状态与进度必须来自实际任务，不能沿用模拟数据。

## 模块化与依赖边界

- 界面经 Tauri 适配层调用任务服务与 Rust 核心；核心不依赖前端、窗口或 Tauri Commands，编码器通过格式适配器隔离，禁止循环依赖和绕过模块接口。
- 扫描、调度、编码、验证和输出各司其职；只有输出层提交最终文件。质量映射与校验等纯逻辑应可独立测试，各格式保留适用的有损与无损路径。
- Rust 维护权威任务状态，前端按功能组织组件、设置草稿和展示快照。批次启动时固定参数，运行中修改设置不改变已有任务。
- 默认隐藏实现细节，Rust 优先使用私有或 `pub(crate)` 可见性；函数与模块职责清晰，按实际复用和测试需要抽象，避免万能工具模块和过度拆分 crate。

## 代码与接口规范

- 标识符使用清晰英文并遵循语言惯例；Rust 函数、变量与模块用 `snake_case`，类型与 trait 用 `UpperCamelCase`。常量与配置明确单位、默认值及有效范围，避免魔法数字。
- 用类型区分任务 ID、质量值、字节数等领域概念，状态采用枚举或可辨识联合。外部路径、图片头、参数与状态在边界校验，静态类型不能替代运行时校验。
- Rust DTO 与 TypeScript 类型保持单一来源生成或自动一致性检查；接口变化同步调用方与契约测试，已发布接口说明兼容或迁移方式。生成代码修改来源或生成器。
- TypeScript 启用严格检查；优先安全 Rust，必要的 `unsafe`／FFI 限于最小封装并验证边界，不以 `any`、无依据断言或 `unsafe` 掩盖类型与模型问题。

## 注释与开发文档

- 项目说明与业务注释使用简洁中文，术语和标识符保留英文；注释随行为更新，删除失效注释及注释掉的旧代码。
- 核心模块用 Rust `//!` 或模块文档说明职责、依赖边界和不变量；公开接口用 Rust `///` 或 TypeScript TSDoc 说明参数与单位、结果、错误、副作用和取消契约，简单内部函数无需机械注释。
- 注释解释设计原因与非直观约束，重点覆盖质量映射、颜色与透明度、动画帧合成、并发及文件提交；必要时引用规范和样本。Rust API 按需补充 `Errors`、`Panics`、`Safety` 与可执行示例。
- 每处 `unsafe` 写明 `SAFETY` 依据；`TODO`／`FIXME` 写清问题、后续动作及关联记录。影响架构或兼容性的决策写入所属文档，不只留在注释中。

## 错误、并发与资源管理

- 可恢复失败通过 `Result` 和可识别错误返回，保留原因与任务上下文；不静默吞错或伪装成功，不对用户输入、解码或 I/O 失败直接 `unwrap`／`expect`。
- 明确文件句柄、临时产物、缩略图、缓存、监听和子进程的所有者及释放时机，覆盖成功、失败、取消和退出路径；核心任务不依赖界面销毁释放。
- 耗时处理进入有界后台任务，同时限制文件并发、编码器线程与解码内存；同步锁不跨 `await` 或耗时 I/O。任务恰好进入一个终态；编码器不支持即时取消时等待返回并丢弃结果，停止等待不等于停止计算。
- 输出先写临时文件并验证，提交前复查取消、源文件变化和目标冲突，覆盖前按策略建立可恢复备份；失败、取消或无收益项保留原图，不用先删原文件再写入的方式覆盖。
- 日志记录任务 ID、阶段、耗时和错误，避免重复记录及泄露图片内容、完整私人路径或凭据；性能优化保留样本、环境与前后对比。

## 工具链、依赖与配置

- 已使用 pnpm、Vite、Oxlint、Prettier、Vitest 与 Cargo workspace；`pnpm desktop:dev` 启动桌面，`pnpm check` 执行统一检查。变更 Rust DTO 后运行 `pnpm types:generate`，CI 用 `pnpm types:check` 只读检查。
- 工程初始化时锁定 Rust、Node 和包管理器版本，在 README 记录实际命令与系统依赖；固定 Rust edition 与格式风格。本地和 CI 使用同一套配置。
- 提交 `Cargo.lock` 与唯一的前端锁文件，使用锁定依赖的安装与构建；依赖升级尽量与功能改动分开，Git 依赖及随包编码工具固定到可复现版本。
- 新增依赖先检查现有能力，评估用途、维护、许可证、平台和构建成本，只启用所需功能；迁移代码与分发第三方工具保留来源、版权和各自许可。
- 配置集中管理并提供默认值、校验和必要迁移；不硬编码个人路径、端口或凭据。Tauri 按需开放最小权限；密钥、私人图片、缓存和构建产物不提交。

## 测试与持续集成

- 按风险选择单元、集成和端到端验证：覆盖质量映射、任务状态、真实图像处理及输出提交；重要行为与缺陷修复补充有效用例，低风险文案无需新增测试。
- 使用可复现图像语料和隔离临时目录，覆盖透明度、位深、ICC／方向、GIF／APNG 时间轴与合成，以及损坏输入、无收益、取消竞争、文件冲突和权限失败。无损验证对应格式的不变量，不只比较文件字节。
- 测试公开行为和边界，避免绑定私有实现、固定休眠或真实网络；时间与随机性可控。核心回归不依赖 GUI，HTML 模拟、前端模拟 IPC 与原生拖放／对话框验证分别说明。
- 工程建立后提供统一格式、类型、lint、测试和构建入口；Rust 纳入 rustfmt、Clippy、测试及适用 doctest，前端纳入格式、类型、lint、测试和构建，生成类型检查可重复生成。
- 若启用提交 hooks，只读检查暂存内容；格式配置变化检查整个索引，不自动格式化、stash 或暂存用户改动，并验证部分暂存场景。hooks 不能替代 CI，远端必需检查与分支保护以实际配置为准。
- Windows 优先验证，按方案逐步纳入 macOS／Linux 构建与打包运行检查；只声明已验证的平台能力。不通过全局关 lint、跳过失败用例或盲目更新快照掩盖问题，局部豁免说明原因，依赖与许可纳入检查或升级评审。

## 验证与交付

- 修改后检查 diff、文档链接与一致性，按实际入口运行受影响范围的检查；仅在新改动、失败或未解决问题需要时扩大或重复验证。
- 界面改动验证相关交互、亮暗主题、中英文及窗口适配；压缩与输出改动使用真实样本检查结果和原图安全，不以模拟进度代替真实验证。
- 交付与 PR 简述问题、最终行为、验证证据和未完成项，区分静态检查、运行验证、视觉验收与性能实测。发布时同步版本、变更和迁移说明，验证打包、安装及启动。
- 未经用户明确要求，不提交或推送代码；交接时区分本机修改、本地提交和远端同步状态。

## devlog 交接

- 开发相关工作必须记录到 `docs/devlog/`，包括功能实现、重构、缺陷修复、架构决策、工具链与依赖配置、测试构建、性能实验及故障排查；相关开发文档并入同一任务记录。`docs/devlog/README.md` 仅维护当前阶段、活动任务及优先顺序。
- 开始开发前创建或续写对应记录，实施中随关键进展更新；不能只在聊天回复中说明，也不要复制完整聊天或工具输出。
- 计划放在 `docs/devlog/_plan/YYMMDD/<任务名>.md`，日期取创建日；完成、取消或被替代后移至 `docs/devlog/_fin/YYMMDD/<任务名>.md`，日期取实际归档日。使用 `Asia/Shanghai` 时区，正文日期用 `YYYY-MM-DD`。
- 每任务保留一份连续记录：维护目标与验收、状态、分支与代码基准、实际改动、关键决策、问题与尝试、验证命令及结果、下一步和阻塞；按日期追加，跨天继续原文件，未执行或未通过的验证如实注明。
- 阶段完成、阻塞或交接前更新记录，写清下一步入口、动作、预期结果及相关未提交文件；提交或推送后补充提交号、分支及实际同步结果。归档保留完整过程，同步索引与相对链接，不改写历史创建或归档日期。

## 参考规范

用于理解工程原则，具体执行以本项目设计和已落地工具配置为准：

- [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/checklist.html)：命名、类型、接口边界、错误与 API 文档。
- [Google Engineering Practices](https://google.github.io/eng-practices/review/reviewer/looking-for.html)：设计、复杂度、测试、命名、注释与文档评审。
