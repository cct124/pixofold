# PixoFold UI 生图提示词

日期：2026-09-20。调用方式：内置 image_gen 工具，使用参考图生成亮色设计，再基于亮色设计生成暗色变体。未使用 CLI/API 脚本或程序绘制替代图。

## 参考与输出

- 亮色生成的参考输入：[png-palettes 原界面](参考-png-palettes.png)，仅参考布局和操作关系。
- 亮色输出：[PixoFold-亮色.png](PixoFold-亮色.png)。
- 暗色编辑的输入：[PixoFold-亮色.png](PixoFold-亮色.png)，保留布局与文案，只调整主题。
- 暗色输出：[PixoFold-暗色.png](PixoFold-暗色.png)。
- 所有文件保存生成原图，不进行代码拼图、重绘、调色或文字覆盖。

## 亮色图完整提示词

```text
Use case: ui-mockup.
Asset type: high-fidelity LIGHT THEME Windows desktop application interface screenshot for PixoFold（轻图）, a local batch image compressor.
Input image 1 is a layout reference only: png-palettes' existing wide left file workspace and right settings sidebar. Create a NEW modern UI, not a pixel-for-pixel edit. Preserve this practical left-workspace/right-settings hierarchy.
Output: one single complete flat app screenshot, landscape 16:10, ideally 1920x1200 pixels, front-facing, crisp readable Simplified Chinese typography. No surrounding computer, no perspective, no presentation board, no multiple screens, no watermark.
Visual design: restrained modern native desktop tool; spacious clean white and very pale cool-gray surfaces, near-black text, readable gray secondary text, one calm blue accent, subtle 1px borders, 10-12px corners, very light shadows. No gradients, glass effects, decorative blobs, large marketing headlines or analytics charts. Use a Chinese sans-serif font similar to Microsoft YaHei UI / Noto Sans SC. Large enough typography to read all controls.
Layout: 56px full-width title bar, with a small simple folded-image blue icon and exact brand text "PixoFold · 轻图" on the left. Top right: compact theme toggle "浅色" "深色" with 浅色 selected, then normal minimize/maximize/close window controls. Below, broad left workspace about 70% of width and fixed right sidebar about 30% width separated by a fine vertical line. Generous 24px paddings.
Left workspace: compact heading "图片压缩". A large, calm dashed-border drop zone in its upper half with a simple outline image-plus icon, heading "拖拽图片或文件夹到这里", then four small neutral format chips "PNG" "JPG" "GIF" "APNG"; below them two buttons "选择文件" and "选择目录". In the lower half, a clean compact task list headed "任务列表", with a small "清空" action. Columns "文件" "大小" "状态". Four aligned rows with small tasteful image thumbnails: "landscape.png" / "3.20 MB" / "待处理"; "travel.jpg" / "6.40 MB" / "待处理"; "motion.gif" / "1.60 MB" / "待处理"; "sticker.apng" / "2.80 MB" / "待处理". All are pending, no fabricated compression results or progress bars. Bottom-left summary "4 个文件 · 14.00 MB" and discreet helper "所有图片仅在本机处理".
Right sidebar: heading "压缩设置", then a two-option segmented control "有损压缩" selected and "无损优化" unselected.
Most prominent settings block: heading "压缩质量" and a small selected-value pill "高".
CRITICAL QUALITY CONTROL: exactly ONE horizontal slider with EXACTLY SIX evenly spaced snap points. Under the six points, print these exact labels from left to right, EACH ONCE: "低", "较低", "中", "高", "较高", "最高". The thumb is on the FOURTH point, directly above "高". The active blue track runs from the first to the fourth point; the remaining two points and track are gray. The "高" label is blue and emphasized. There must be exactly six labels, with no missing, repeated, or reordered tiers.
Directly UNDER this slider, show label "精细调整", an editable numeric input visibly containing "80", and a small suffix "0–100". Below, one short helper line "数值越高，画质保留越多". Default quality is 高 = 80. Do not use a dropdown for quality. Do not add separate quality sliders for different formats.
Below a thin divider: "输出设置", a selected option "另存副本", then "输出目录" and a neat folder-picker field reading "原文件夹". One collapsed disclosure row "高级选项" with a chevron.
Anchor a wide blue primary button "开始压缩" near the bottom of the sidebar. At the very bottom, quiet controls "简体中文" and "关于".
All requested Chinese UI text should be rendered faithfully, particularly 轻图 and the six quality tier labels. No raw codec names, source code, GPL labels or technical licensing text in the product UI. Keep the frame uncluttered and professional.
```

## 暗色图完整提示词

```text
Use case: ui-mockup / theme variant.
Input image 1 is the EXACT edit target: the approved PixoFold（轻图）light-theme desktop UI. Produce its DARK-THEME counterpart.
Change ONLY the color theme and selected theme toggle. Preserve every component's geometry, layout, window size, alignment, typography size, Chinese wording, thumbnails, filenames, numbers, table rows, controls, and spacing exactly as the reference. Output a single complete flat app screenshot at the same landscape aspect ratio and resolution. Do not create a comparison collage or put the app in a device.
Dark palette: near-black charcoal canvas #111318, slightly lighter surfaces #191D24, secondary surfaces #222833, subtle borders #343D4B, primary text #F1F5F9, secondary text #AAB6C5. Maintain a calm blue accent for the active slider, selected control and focus. Primary button uses a saturated medium/deep blue with legible white text; no white panels or pure-black high-contrast patches. No glow, neon, glass effects or decorative gradients. Retain the image thumbnails' original colors.
The title bar's "浅色" / "深色" toggle must now select "深色" and make "浅色" unselected. Both remain readable. Brand text stays exactly "PixoFold · 轻图".
Critical invariants: "有损压缩" remains selected. Quality heading "压缩质量" and selected pill "高" remain. Exactly SIX slider points and EXACTLY these labels in this order: "低", "较低", "中", "高", "较高", "最高". Thumb remains at the FOURTH point above "高"; blue track only to that fourth point; two remaining points are inactive. Numeric input below still shows "80", label "精细调整", suffix "0–100", helper "数值越高，画质保留越多".
Keep "另存副本" selected, output "原文件夹", "高级选项" collapsed, main action "开始压缩".
Keep all four pending files unchanged: landscape.png 3.20 MB, travel.jpg 6.40 MB, motion.gif 1.60 MB, sticker.apng 2.80 MB. Preserve total "4 个文件 · 14.00 MB", format chips PNG / JPG / GIF / APNG, import area and both choose buttons. Preserve all other readable UI text.
Make the result look like the same shippable minimalist Windows app switched to dark mode. No watermark, no code, no explanatory captions outside the application.
```
